# lesson08-lab Answers

# Part 1: Object declarations
2. **Which files are generated by the Kotlin compiler?**    
R -> 3 files are generated by the compiler:
    - `Problem.class`
    - `ProblemEmailAlreadyInUse.class`
    - `ProblemUserOrPasswordAreInvalid.class`

3. **Which members are generated in the ProblemEmailAlreadyInUse class?**  
Identify each kind of member and explain its purpose.  
R -> 3 members are generated in the class:
    - ``Instance`` - Type: `Field` and ``Defines the class as an Objetct``
    - ``ProblemEmailAlreadyInUse`` - Type: `Private Constructor` and `Private constructor to prevent the creation of multiple instances`
    - ``Static`` - Type: `Initializer` and `Serves as the initializer of the object`

4. **Make an equivalent Java implementation of the ProblemEmailAlreadyInUse Kotlin object declaration that produces the same metadata as the Kotlin version.**  
Your ProblemEmailAlreadyInUse Java class should extend the existing Problem class created from the Problem Kotlin class declaration.  
R -> `ProblemEmailAlreadyInUse.java`:
    ```
    public final class ProblemEmailAlreadyInUse extends Problem {

        public static final ProblemEmailAlreadyInUse INSTANCE;

        private ProblemEmailAlreadyInUse() {
            super("Email Already In Use", "There is already a user with given email");
        }

        static {
            INSTANCE = new ProblemEmailAlreadyInUse();
        }
    }
    ```

5. **Use the javap -p -c to verify that the generated class file is similar to that one produced by the Kotlin compiler.**    
R -> **Kotlin Compiler**:
    ```
    public final class ProblemEmailAlreadyInUse extends Problem {
        public static final ProblemEmailAlreadyInUse INSTANCE;

        private ProblemEmailAlreadyInUse();
            Code:
                0: aload_0
                1: ldc           #1                  // String Email Already In Use
                3: ldc           #3                  // String There is already a user with given email
                5: invokespecial #5                  // Method Problem."<init>":(Ljava/lang/String;Ljava/lang/String;)V
                8: return

        static {};
            Code:
                0: new           #11                 // class ProblemEmailAlreadyInUse
                3: dup
                4: invokespecial #13                 // Method "<init>":()V
                7: putstatic     #16                 // Field INSTANCE:LProblemEmailAlreadyInUse;
                10: return
    }
    ```
    **Java Compiler**:
    ```
    public final class ProblemEmailAlreadyInUse extends Problem {
        public static final ProblemEmailAlreadyInUse INSTANCE;

        private ProblemEmailAlreadyInUse();
            Code:
                0: aload_0
                1: ldc           #1                  // String Email Already In Use
                3: ldc           #3                  // String There is already a user with given email
                5: invokespecial #5                  // Method Problem."<init>":(Ljava/lang/String;Ljava/lang/String;)V
                8: return

        static {};
            Code:
                0: new           #11                 // class ProblemEmailAlreadyInUse
                3: dup
                4: invokespecial #13                 // Method "<init>":()V
                7: putstatic     #16                 // Field INSTANCE:LProblemEmailAlreadyInUse;
                10: return
    }
    ```

6. **Confirm that System.out.println(ProblemEmailAlreadyInUse.INSTANCE); prints:**  
`Problem(title='Email Already In Use', desc='There is already a user with given email')`  
Note that you must run this example using the kotlin command line tool, not java, since your code depends on the Problem class generated by the Kotlin compiler.
Otherwise using java you will need to include the kotlin standard library in the classpath.  
R -> **Main.kt**:
    ```
    fun main() {
        println(ProblemEmailAlreadyInUse.INSTANCE)
    }
    ```
    ```
    Expected: 
    Problem(title='Email Already In Use', desc='There is already a user with given email')

    Output we get:
    Problem(title='Email Already In Use', desc='There is already a user with given email')
    ```

# Part 2: Companion objects
2. **Which class files are generated by the Kotlin compiler?**  
R -> 2 class files are generated: `Dice.class` and `Dice$Companion.class`.

3. **Which members are generated in each of the generated class files?**  
Identify each kind of member and explain its purpose.  
R -> **Dice.class**:
    - ``Dice$Companion`` - Type: ``Field`` and Creates a companion object
    - ``getSharedRandom`` - Type: ``Method`` and Generates a random number
    - ``Dice$Companion(kotlin.jvm.internal.DefaultConstructorMarker)`` - Type: ``Constructor`` and Default constructor of the Dice class

    **Dice$companion.class**:
    - ``Dice$Companion`` - Type: ``Private Constructor`` and ``Private constructor to prevent the creation of multiple instances``
    - ``getSharedRandom`` - Type: ``Method`` and ``Returns the shared Random instance``
    - ``Dice$Companion(kotlin.jvm.internal.DefaultConstructorMarker)`` - Type: ``Constructor`` and ``Default constructor of the Dice$companion class``

4. **How in Java would you access the sharedRandom property of the Dice companion object?**    
R -> Through the companion object: `Dice.Companion.getSharedRandom();`

# Part 3: Function Types
1. **Copy the above code into a file named Utils.kt and compile it using the command line with kotlinc Utils.kt. Try to predict the output of the program before executing it.**  
Execute the program using kotlin UtilsKt and confirm that the output matches your expectations.  
R -> **Expected**:
    ```
    Crossing ola
    3
    Crossing isel
    4
    Crossing super
    5
    ```
    **Output**:
    ```
    Crossing ola
    Crossing isel
    Crossing super
    3
    4
    5
    ```

2. **Analyze each part of the Kotlin source code identifying each lambda in use.**  
R -> 
    - **1st Lambda**: 
    ```
    {
        println("Crossing $it")
        transform(it)
    }
    ```
    - **2st Lambda**: ``{ it.toString().length }``

3. **Analyze the metadata of UtilsKt using javap -p UtilsKt.class and identify what is the type of the transform parameter in the cross method?**  
R -> The type of the `transform` parameter is: `kotlin.jvm.functions.Function1<java.lang.Object, java.lang.Integer>`.

4. **Analyze and register the metadata of the type of the transform parameter using the next command, replacing <Type of transform parameter> with the actual type identified in the previous step.**  
Note that you will need to include the kotlin standard library in the classpath when running javap.
You can use the following command, replacing <full path> with the actual path to your kotlin-stdlib.jar file:  
`javap -cp ".;<full path>\kotlin-stdlib.jar" <Type of transform parameter>`  
R -> **CMD**:
    ```
    javap -cp ".;C:\kotlinc\lib\kotlin-stdlib.jar" kotlin.jvm.functions.Function1
    
    Compiled from "Functions.kt"
    public interface kotlin.jvm.functions.Function1<P1, R> extends kotlin.Function<R> {
        public abstract R invoke(P1);
    }
    ```
    We have an `invoke` method that works as a function taking `P1` as an argument returning into a result, stored in `R`.

5. **Analyze the metadata of the UtilsKt.class using javap -p UtilsKt.class and identify what is the member resulting from the lambda expression used in the main function.**  
R -> The member resulting from the lambda expression in the main function is `main$lambda$1(java.lang.Object)`. It is an ``object`` and it's purpose is to implement the transformation ``<Object>`` -> ``Int``.

6. **Add the next usage of the cross function in the main function,**  
    ```
    cross(listOf(true, false, true)) {
        if (it == true) 1 else 0
    }
   .forEach(::println)
    ```
    **Main Function:**
    ```
    fun main() {
        cross(listOf("ola", "isel", "super")) { it.toString().length }
            .forEach(::println)

        println("6th Step")

        cross(listOf(true, false, true)) {
            if (it == true) 1 else 0
        }
        .forEach(::println)
    }
    ```

7. **Compile and execute the program again and confirm that the output matches your expectations.**  
**Expected**:
    ```
    Crossing ola
    Crossing isel
    Crossing super
    3
    4
    5
    6th Step
    Crossing true
    Crossing false
    Crossing true
    1
    0
    1
    ```
    The output matched the expected result.

8. **Analyze the metadata of the UtilsKt.class again using javap -p UtilsKt.class and identify what is the member resulting from the new lambda expression used in the main function.**  
R -> The member who resulted from the new lambda expression is `main$lambda$2`.

9. **Note that the anonymous functions resulting from the lambda expressions used in the main function are generated as static members of the UtilsKt class.**  
Note these members are not accessible to the Kotlin or Java source code directly, but they are used by the cross function when it is called with a lambda expression as argument. Even so, you can use reflection to access these members and invoke them directly.  
For example, you can use the next code snippet to access the member resulting from the first lambda expression used in the main function, which is the one that transforms a string into its length.  
Calling the funLength through reflection, e.g. with funLength.invoke(null, "test") should return 4.
This confirms that the funLength represents the function resulting from the first lambda expression used in the main function, which transforms a string into its length.  
    ```
    // Replace <Name of first lambda> with the actual name of the member resulting 
    // from the first lambda expression.
    //
    val funLength = Class
        .forName("UtilsKt")
        .getDeclaredMethod("<Name of first lambda>", Any::class.java) 
        .apply { isAccessible = true }
    ```

10. **Build another code snippet to access the member resulting from the second lambda expression used in the main function, which is the one that transforms a boolean into an integer.**
    ```
    val funBoolean = Class
        .forName("UtilsKt")
        .getDeclaredMethod("main$lambda$2", java.lang.Boolean.TYPE)
        .apply { isAccessible = true }

    val invoke = funBoolean.invoke(null, true)
    ```